server
{
    listen 80;
    listen 443 ssl http2 ;
    listen [::]:443 ssl http2 ;
    listen [::]:80;
    server_name ispvpn.vive3d.cl;
    index index.php index.html index.htm default.php default.htm default.html;
    root /www/wwwroot/ispvpn.vive3d.cl;
    include /www/server/panel/vhost/nginx/extension/ispvpn.vive3d.cl/*.conf;

    #SSL-START SSL related configuration, do NOT delete or modify the next line of commented-out 404 rules
    #error_page 404/404.html;
    ssl_certificate    /www/server/panel/vhost/cert/ispvpn.vive3d.cl/fullchain.pem;
    ssl_certificate_key    /www/server/panel/vhost/cert/ispvpn.vive3d.cl/privkey.pem;
    ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_tickets on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    add_header Strict-Transport-Security "max-age=31536000";
    error_page 497  https://$host$request_uri;
    #SSL-END

    #ERROR-PAGE-START  Error page configuration, allowed to be commented, deleted or modified
    #error_page 404 /404.html;
    #error_page 502 /502.html;
    #ERROR-PAGE-END

    #PHP-INFO-START  PHP reference configuration, allowed to be commented, deleted or modified
    #include enable-php-83.conf;
    #PHP-INFO-END

    #REWRITE-START URL rewrite rule reference, any modification will invalidate the rewrite rules set by the panel
    include /www/server/panel/vhost/rewrite/ispvpn.vive3d.cl.conf;
    #REWRITE-END

    # Forbidden files or directories
    location ~ ^/(\.user.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README.md)
    {
        return 404;
    }

    # Directory verification related settings for one-click application for SSL certificate
    location ~ \.well-known{
        allow all;
    }

    #Prohibit putting sensitive files in certificate verification directory
    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }

    # VPN_PUBLIC_HOST: OpenVPN/WireGuard NO pasan por nginx.
    # Este vhost solo mantiene el certificado SSL / ACME del hostname.
    # Puertos reales (firewall / DNAT al concentrador):
    #   OpenVPN TCP 1194 | OpenVPN UDP 1195 | WireGuard UDP 51820
    location /
    {
        default_type text/plain;
        return 200 "ISP Control VPN endpoint (ispvpn.vive3d.cl).\nOpenVPN/WireGuard are not HTTP — use ports 1194/1195/51820.\n";
    }

    access_log  /www/wwwlogs/ispvpn.vive3d.cl.log;
    error_log  /www/wwwlogs/ispvpn.vive3d.cl.error.log;
}
